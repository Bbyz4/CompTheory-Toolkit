#!/bin/sh
set -eu

root_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root_dir"

if [ "${IN_DEV_CONTAINER:-}" = "1" ]; then
  exec opam exec -- dune exec apps/admincli/admincli.exe -- "$@"
fi

./scripts/ensure_docker.sh

if ./bin/compose ps --status running --services 2>/dev/null | grep -qx 'app'; then
  exec ./bin/compose exec -T app /app/admincli.exe "$@"
fi

exec ./bin/compose run --rm app /app/admincli.exe "$@"
